{
  "dependencies": {
    "@observablehq/notebook-kit": "^1.5.1"
  },
  "scripts": {
    "docs:preview": "notebooks preview --root notebooks",
    "docs:build": "notebooks build --empty --root notebooks --out ../docs -- notebooks/*.html notebooks/*.js"
  }
}
